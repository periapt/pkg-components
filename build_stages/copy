#!/usr/bin/make -f

main: check COMPONENTS/${DH_COMPONENT}

check:
	@test -n ${DH_COMPONENT}

COMPONENTS/${DH_COMPONENT}: COMPONENTS ${DH_COMPONENT}
	cp -R ${DH_COMPONENT} COMPONENTS

COMPONENTS:
	mkdir -p COMPONENTS

